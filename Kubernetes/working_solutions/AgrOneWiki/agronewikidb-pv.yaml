apiVersion: v1
kind: PersistentVolume
metadata:
  name: agronewikidb-pv
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: "/kubernetes/agronewiki/agronewikidb/pv"
