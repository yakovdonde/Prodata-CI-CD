apiVersion: v1
kind: Service
metadata:
  name: qgis-server-service
  namespace: agrotech-dev
spec:
  type: ClusterIP
  selector:
    myLabel: agrogis-server
  ports:
    - port: 5555
      targetPort: 5555
#      name: fastcgi