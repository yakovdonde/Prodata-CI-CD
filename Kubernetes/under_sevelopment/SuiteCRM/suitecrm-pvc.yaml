apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: suitecrm-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi  # Should match the PV capacity
  selector:
    matchLabels:
      app: suitecrm