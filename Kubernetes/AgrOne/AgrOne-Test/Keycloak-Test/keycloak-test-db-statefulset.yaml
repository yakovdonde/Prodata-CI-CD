apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keycloak-test-db
  namespace: agrotech-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak-test-db
  template:
    metadata:
      labels:
        app: keycloak-test-db
    spec:
      containers:
        - name: keycloak-test-db
          image: postgres
          imagePullPolicy: Always
          env:
            - name: POSTGRES_DB
              value: keycloak
            - name: POSTGRES_USER
              value: keycloak
            - name: POSTGRES_PASSWORD
              value: keycloak
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: keycloak-test-db-storage
              mountPath: /var/lib/postgresql/data

      volumes:
        - name: keycloak-test-db-storage
          persistentVolumeClaim:
            claimName: keycloak-test-db-pvc