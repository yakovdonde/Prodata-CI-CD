
apiVersion: v1
kind: Service
metadata:
  name: keycloak-test-service
  namespace: agrotech-test
spec:
  type: NodePort
  selector:
    app: keycloak-test
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
