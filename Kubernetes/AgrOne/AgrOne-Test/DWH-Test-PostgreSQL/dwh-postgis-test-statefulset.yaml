---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: dwh-postgis-test-db
  namespace: agrotech-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dwh-postgis-test-db
  template:
    metadata:
      labels:
        app: dwh-postgis-test-db
    spec:
      containers:
      - name: dwh-postgis-test-db
        image: postgis/postgis
        env:
          - name: POSTGRES_DB
            value: "dwh"  # Replace with your PostgreSQL database name
          - name: POSTGRES_USER
            value: "dwh"  # Replace with your PostgreSQL username
          - name: POSTGRES_PASSWORD
            value: "dwh"  # Replace with your PostgreSQL password
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: dwh-postgis-test-db-pvc
          mountPath: "/var/lib/postgresql/data"
      volumes:
      - name: dwh-postgis-test-db-pvc
        persistentVolumeClaim:
          claimName: dwh-postgis-test-db-pvc
