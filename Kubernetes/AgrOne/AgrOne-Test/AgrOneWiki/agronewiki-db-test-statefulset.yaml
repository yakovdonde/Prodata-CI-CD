---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: agronewiki-db-test
  namespace: agrotech-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agronewiki-db-test
  template:
    metadata:
      labels:
        app: agronewiki-db-test
    spec:
      containers:
      - name: agronewiki-db-test
        image: postgres
        env:
          - name: POSTGRES_DB
            value: "xwiki"  # Replace with your PostgreSQL database name
          - name: POSTGRES_USER
            value: "xwiki"  # Replace with your PostgreSQL username
          - name: POSTGRES_PASSWORD
            value: "xwiki"  # Replace with your PostgreSQL password
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: agronewiki-db-test-pvc
          mountPath: "/var/lib/postgresql/data"
      volumes:
      - name: agronewiki-db-test-pvc
        persistentVolumeClaim:
          claimName: agronewiki-db-test-pvc