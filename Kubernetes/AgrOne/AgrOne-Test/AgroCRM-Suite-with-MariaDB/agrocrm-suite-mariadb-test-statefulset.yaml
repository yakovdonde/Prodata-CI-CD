apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: agrocrm-suite-mariadb-test
  namespace: agrotech-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agrocrm-suite-mariadb-test
  template:
    metadata:
      labels:
        app: agrocrm-suite-mariadb-test
    spec:
      initContainers:
        - name: mariadb-create-directory-structure
          image: busybox
          command:
            [
              "sh",
              "-c",
              "mkdir -p /bitnami/mariadb/data && chown -R 1001:1001 /bitnami",
            ]
          volumeMounts:
            - name: agrocrm-suite-mariadb-test-persistent-storage
              mountPath: /bitnami
      containers:
        - name: agrocrm-suite-mariadb-test
          image: bitnami/mariadb
          env:
            - name: ALLOW_EMPTY_PASSWORD
              value: "yes"
            - name: MARIADB_DATABASE
              value: "suitecrm"
            - name: MARIADB_USER
              value: "suitecrm"
            - name: MARIADB_PASSWORD
              value: "suitecrm"
          ports:
            - containerPort: 3306
          volumeMounts:
            - mountPath: /bitnami/mariadb
              name: agrocrm-suite-mariadb-test-persistent-storage
      volumes:
        - name: agrocrm-suite-mariadb-test-persistent-storage
          persistentVolumeClaim:
            claimName: agrocrm-suite-mariadb-test-pvc
