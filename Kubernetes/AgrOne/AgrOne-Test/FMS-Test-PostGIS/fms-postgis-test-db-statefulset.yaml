---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: fms-postgis-test-db
  namespace: agrotech-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fms-postgis-test-db
  template:
    metadata:
      labels:
        app: fms-postgis-test-db
    spec:
      containers:
      - name: fms-postgis-test-db
        image: postgis/postgis
        env:
          - name: POSTGRES_DB
            value: "fms"  # Replace with your PostgreSQL database name
          - name: POSTGRES_USER
            value: "fms"  # Replace with your PostgreSQL username
          - name: POSTGRES_PASSWORD
            value: "fms"  # Replace with your PostgreSQL password
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: fms-postgis-test-db-pvc
          mountPath: "/var/lib/postgresql/data"
      volumes:
      - name: fms-postgis-test-db-pvc
        persistentVolumeClaim:
          claimName: fms-postgis-test-db-pvc
