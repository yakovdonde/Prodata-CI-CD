apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak-prod-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  namespace: agrotech-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - 'keycloak.prodata.local'
      secretName: tls-secret-prodata-local
  rules:
  - host: keycloak.prodata.local
    http:
      paths:
      - backend:
          service:
            name: keycloak-prod-service
            port:
              number: 8080
        path: /
        pathType: Prefix