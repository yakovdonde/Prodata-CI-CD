apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pro-rti-db-dev
  namespace: agrotech-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pro-rti-db-dev
  template:
    metadata:
      labels:
        app: pro-rti-db-dev
    spec:
      containers:
      - name: pro-rti-db-dev
        image: postgres
        env:
        - name: POSTGRES_USER
          value: "rti"
        - name: POSTGRES_PASSWORD
          value: "rti"
        - name: POSTGRES_DB
          value: "rti"
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: pro-rti-db-dev-pvc
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: pro-rti-db-dev-pvc
        persistentVolumeClaim:
          claimName: pro-rti-db-dev-pvc
