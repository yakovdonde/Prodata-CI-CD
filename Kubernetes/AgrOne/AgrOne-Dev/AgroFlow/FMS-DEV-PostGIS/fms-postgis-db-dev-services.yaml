apiVersion: v1
kind: Service
metadata:
  name: fms-postgis-db-dev-nodeport-service
  namespace: agrotech-dev
spec:
  type: NodePort
  selector:
    app: fms-postgis-db-dev
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
    nodePort: 30004  # You can choose any available port on your nodes

---

apiVersion: v1
kind: Service
metadata:
  name: fms-postgis-db-dev-clusterip-service
  namespace: agrotech-dev
spec:
  type: ClusterIP
  selector:
    app: fms-postgis-db-dev
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432