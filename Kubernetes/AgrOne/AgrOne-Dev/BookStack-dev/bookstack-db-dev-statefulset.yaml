apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bookstack-db-dev
  namespace: agrotech-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bookstack-db-dev
  template:
    metadata:
      labels:
        app: bookstack-db-dev
    spec:
      containers:
        - name: bookstack-db-dev
          image: postgres
          env:
            - name: POSTGRES_DB
              value: bookstack
            - name: POSTGRES_USER
              value: bookstack
            - name: POSTGRES_PASSWORD
              value: bookstack
          ports:
            - containerPort: 5432
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: bookstack-db-dev-persistent-storage
      volumes:
        - name: bookstack-db-dev-persistent-storage
          persistentVolumeClaim:
            claimName: bookstack-db-dev-pvc